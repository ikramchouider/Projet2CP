/*
class RI {
    remplirRI = (code) => {
    cop = code.substring(0, 5);
    ma  = code.substring(6, 7);
    f  = code.substring(8, 8);
    d  = code.substring(9 , 9);
    reg1 = code.substring(10,12);
    reg2 = code.substring(13,15);
    }
trouverMA =(ma) => {
    if (ma == "00") { console.log("le mode d'adressage est ")}
}
 decodage = () => {

}
   
  }*/
  let y='yasmine';
  console.log(y);

  
  